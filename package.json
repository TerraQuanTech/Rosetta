{
	"name": "rosetta-i18n",
	"version": "0.3.4",
	"description": "A visual i18n translation editor for JSON locale files, with live preview support for Electron apps",
	"workspaces": ["packages/*"],
	"scripts": {
		"start": "vite build && electrobun dev",
		"dev": "vite build && electrobun dev --watch",
		"dev:hmr": "concurrently \"bun run hmr\" \"bun run start\"",
		"hmr": "vite --port 5174",
		"build": "vite build && electrobun build --env=stable",
		"build:canary": "vite build && electrobun build --env=canary",
		"test": "bun test",
		"test:watch": "bun test --watch",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"typecheck": "bunx tsc --noEmit",
		"build:vscode": "vite build --config packages/vscode-extension/vite.config.webview.ts && node packages/vscode-extension/esbuild.config.mjs"
	},
	"dependencies": {
		"@leeoniya/ufuzzy": "^1.0.19",
		"@terraquantech/rosetta-core": "workspace:*",
		"chokidar": "^4.0.0",
		"commander": "^12.1.0",
		"electrobun": "latest",
		"react": "^18.3.1",
		"react-dom": "^18.3.1"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"@types/bun": "latest",
		"@types/react": "^18.3.12",
		"@types/react-dom": "^18.3.1",
		"@types/three": "^0.183.1",
		"@vitejs/plugin-react": "^4.3.4",
		"concurrently": "^9.1.0",
		"rcedit": "^4.0.1",
		"typescript": "^5.7.2",
		"vite": "^6.0.3"
	},
	"license": "MIT"
}
